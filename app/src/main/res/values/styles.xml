<resources>

    <style name="AppTheme.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="android:overScrollMode">never</item>
    </style>

    <style name="AppTheme" parent="AppTheme.Base"></style>

    <style name="CustomTabLayout" parent="Widget.Design.TabLayout">
        <item name="tabIndicatorHeight">@dimen/dp0</item>
        <!--<item name="tabPaddingStart">@dimen/dp5</item>-->
        <!--<item name="tabPaddingEnd">@dimen/dp5</item>-->
        <item name="tabBackground">@color/colorPrimary</item>
        <item name="tabSelectedTextColor">@android:color/white</item>
        <item name="tabTextColor">@color/gray1</item>
        <item name="tabIndicatorColor">@android:color/white</item>
        <item name="android:textSize">@dimen/font_m</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

</resources>
